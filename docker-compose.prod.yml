services:
  frontend:
    image: campus-club-frontend-prod
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api/v1}
    ports:
      - "5173:80"
    restart: unless-stopped
